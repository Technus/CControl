--List of Fuctions for redstone interactions

--adds ON-OFF redstone (analog) and togglable by impulse redstone IO's restone to database
function addIO(id,name,descr,method,dirval)--type
  
end

--init phase

os.run("restoneDB")
